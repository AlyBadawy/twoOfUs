apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: websites

resources:
- deployment.yaml
- service.yaml
- ingress.yaml
- external-secret.yaml

# CI updates newTag on every push to main via `kustomize edit set image`
images:
- name: ghcr.io/alybadawy/twoofus
  newName: ghcr.io/alybadawy/twoofus
  newTag: ab713d25cd013cd63008505e40b873a141b1f3e0
