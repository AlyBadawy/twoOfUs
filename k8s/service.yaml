apiVersion: v1
kind: Service
metadata:
  name: twoofus
  namespace: twoofus
  labels:
    app.kubernetes.io/name: twoofus
    homelab/stack: twoofus
spec:
  selector:
    app: twoofus
  ports:
    - name: http
      port: 80
      targetPort: 8080
